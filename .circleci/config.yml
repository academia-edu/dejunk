version: 2.1

jobs:
  build:
    docker:
      - image: cimg/ruby:3.4.8
    steps:
      - checkout
      - run: bundle install
      - run: bundle exec rspec

workflows:
  build_and_test:
    jobs:
      - build
