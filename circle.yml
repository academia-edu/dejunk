# CircleCI: Hooks mysteriously don't work, matrix builds aren't even supported
#
# I wish we just used Travis

machine:
  ruby:
    version: 2.2.3

dependencies:
  pre:
    - gem install bundler -v 1.10.6
